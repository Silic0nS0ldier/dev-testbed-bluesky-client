# Optimised Vendor Code

This package processes third party dependencies by;
- Pre-bundling to;
  - Keep hot-reload workflow fast.
  - Shard the build for improved cache hit rates.
- Converting CJS sources to spec-compliant ESM.
